package com.github.se.cyrcle.ui.theme.molecules

import androidx.compose.runtime.Composable

/**
 * Create a themed date picker, with simplified arguments. The value contained in the date field
 * must be declared as follow before calling this function :
 * ```
 * var date by remember { mutableStateOf(???) }
 * DatePicker(onValueChange = { newDate -> date = newDate })
 * ```
 *
 * WARNING : Not yet implemented, signature may change.
 */
@Composable
fun DatePicker() {
  TODO()
}
